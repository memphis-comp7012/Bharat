<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "iHunt" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "iHunt" %>">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="action-<%= controller.action_name%>">
<header>
  <%= render 'layouts/navigation' %>
</header>
<main role="main" class="container">
  <div class="row">
    <div class="col-lg-12">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>
  </div>
</main>
<footer>
  <div class="container">
    <div class="row">
      <section class="col-sm-12 col-lg-6 text-muted">
        &copy <%= Date.today.year %>, iHunt. All rights reserved.
      </section>
      <section class="col-sm-12 col-lg-6 text-right">
        Made with <span class="glyphicon glyphicon-heart text-danger" aria-hidden="true"></span> by Team Bharat.
      </section>
    </div>
  </div>
</footer>
</body>
</html>
