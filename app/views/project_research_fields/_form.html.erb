<%= form_for(@project_research_field) do |f| %>
  <% if @project_research_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_research_field.errors.count, "error") %> prohibited this project_research_field from being saved:</h2>

      <ul>
      <% @project_research_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
